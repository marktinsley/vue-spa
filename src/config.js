// Debugging on?
export const debug = process.env.NODE_ENV !== 'production'

// The URL that URI's will be prefixed with.
export const apiBaseUrl = 'http://gottanabit.app'

// The default number of seconds to cache results from API calls.
export const defaultApiCacheSeconds = 900
